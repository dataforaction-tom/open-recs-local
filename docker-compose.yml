services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: openrecs
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d openrecs"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: app-runtime
    restart: unless-stopped
    env_file: .env
    environment:
      # Override any host-oriented DATABASE_URL in .env: inside the compose
      # network the postgres service is reachable at hostname `postgres`,
      # not `localhost`. Placed after env_file so this wins.
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/openrecs
    ports:
      - "3000:3000"
    volumes:
      - uploads:/data/uploads
    depends_on:
      postgres:
        condition: service_healthy

  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: worker-runtime
    restart: unless-stopped
    env_file: .env
    environment:
      # See note on `app.environment` above.
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/openrecs
    volumes:
      - uploads:/data/uploads
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
  uploads:
