import type { ReactNode } from 'react';

// Placeholder — Phase 8 wires Better-auth into this group.
export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
